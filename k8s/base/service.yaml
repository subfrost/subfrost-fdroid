apiVersion: v1
kind: Service
metadata:
  name: fdroid
  namespace: subfrost-fdroid
spec:
  selector:
    app: fdroid
  ports:
    - port: 80
      targetPort: 80
      name: http
    - port: 443
      targetPort: 443
      name: https
  type: LoadBalancer
